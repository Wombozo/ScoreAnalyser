<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:ScoreAnalyser.ViewModels"
             xmlns:views="clr-namespace:ScoreAnalyser.Views"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="ScoreAnalyser.Views.ScoreView">

    <Design.DataContext>
        <viewModels:ScoreViewModel />
    </Design.DataContext>

    <TabControl Items="{Binding Path=ScorePagesVMTabItem}" Name="TabControl">
        <TabControl.ItemTemplate>
            <DataTemplate>
                <TextBlock Text="{Binding Path=PageNumber}" />
            </DataTemplate>
        </TabControl.ItemTemplate>
        <TabControl.ContentTemplate>
            <!-- this is the body of the TabItem template-->
            <DataTemplate>
                <views:ScorePageView/>
            </DataTemplate>
        </TabControl.ContentTemplate>
    </TabControl>
</UserControl>